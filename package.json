{
  "name": "todo-list",
  "type": "module",
  "private": true,
  "version": "0.0.1",
  "description": "A timeline for tracking completed works and releases",
  "scripts": {
    "dev": "rm -rf .dev && tsc -p tsconfig.dev.json && concurrently --no-color --kill-others-on-fail --names tsc,server \"tsc -p tsconfig.dev.json --watch --preserveWatchOutput\" \"node scripts/dev.ts\"",
    "dev:bridge": "rm -rf .dev && tsc -p tsconfig.dev.json && concurrently --no-color --kill-others-on-fail --names tsc,server \"tsc -p tsconfig.dev.json --watch --preserveWatchOutput\" \"node scripts/dev.ts --catalog-bridge\"",
    "start": "npm run dev",
    "build": "rm -rf dist && tsc -p tsconfig.build.json && node scripts/build.ts",
    "typecheck": "tsc --noEmit",
    "lint": "npm run typecheck",
    "test": "node --test src/todo-list/Catalog/model/catalog.test.ts src/todo-list/Catalog/model/timeline.test.ts src/todo-list/Catalog/model/transfer.test.ts src/todo-list/bridge/catalog.test.ts src/todo-list/db/migrate.test.ts scripts/catalog-bridge/server.test.ts",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "check": "npm run typecheck && npm test && npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/zunsthy/todo-list.git"
  },
  "keywords": [
    "todo-list",
    "todo",
    "beginner"
  ],
  "author": {
    "name": "z",
    "url": "https://github.com/zunsthy"
  },
  "engines": {
    "node": ">= 22",
    "npm": ">= 10"
  },
  "license": "MPL-2.0",
  "bugs": {
    "url": "https://github.com/zunsthy/todo-list/issues"
  },
  "devDependencies": {
    "@types/mustache": "^4",
    "@types/node": "^24",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "@types/ws": "^8.18.1",
    "concurrently": "^10",
    "mustache": "^4",
    "prettier": "^3",
    "typescript": "^7"
  },
  "dependencies": {
    "react": "^19",
    "react-dom": "^19",
    "ws": "^8"
  }
}
